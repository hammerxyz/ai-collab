# 项目违规日志  *(Project Violation Log)*

此目录存放本项目的违规事件日志（JSONL 格式）。

详见根目录 [VIOLATIONS/README.md](../../VIOLATIONS/README.md) 与 [SCHEMAS/violation.schema.json](../../SCHEMAS/violation.schema.json)。

**写入权**：由验收方（TEST/QA/WATCHDOG/人类）写入。actor 对自己只有读权限。
